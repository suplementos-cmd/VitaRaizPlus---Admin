@page "/clientes"
@using VitaRaiz.Infrastructure.Data
@inject VitaRaizDbContext Context
@rendermode InteractiveServer

<PageTitle>Clientes - VitaRaiz</PageTitle>

<h1 class="mb-4">👥 Gestión de Clientes</h1>

<div class="row mb-3">
    <div class="col-md-8">
        <input type="text" class="form-control" placeholder="Buscar cliente..." @bind="searchTerm" @bind:event="oninput" />
    </div>
    <div class="col-md-4">
        <button class="btn btn-primary w-100" @onclick="NuevoCliente">
            ➕ Nuevo Cliente
        </button>
    </div>
</div>

@if (clientes == null)
{
    <p><em>Cargando clientes...</em></p>
}
else
{
    <div class="table-responsive">
        <table class="table table-striped table-hover">
            <thead>
                <tr>
                    <th>ID</th>
                    <th>Nombre</th>
                    <th>Teléfono</th>
                    <th>Zona</th>
                    <th>Estado</th>
                    <th>Acciones</th>
                </tr>
            </thead>
            <tbody>
                @foreach (var cliente in clientesFiltrados)
                {
                    <tr>
                        <td>@cliente.CustomerId</td>
                        <td>
                            @cliente.CustomerName
                            @if (cliente.IsGoldCustomer)
                            {
                                <span class="badge bg-warning text-dark">⭐ Gold</span>
                            }
                            @if (cliente.IsBlacklisted)
                            {
                                <span class="badge bg-danger">🚫 Blacklist</span>
                            }
                        </td>
                        <td>@cliente.Phone</td>
                        <td>@(cliente.Zone?.ZoneName ?? "Sin zona")</td>
                        <td>
                            @if (cliente.IsBlacklisted)
                            {
                                <span class="text-danger">Inactivo</span>
                            }
                            else
                            {
                                <span class="text-success">Activo</span>
                            }
                        </td>
                        <td>
                            <button class="btn btn-sm btn-info" @onclick="() => VerDetalle(cliente.CustomerId)">Ver</button>
                            <button class="btn btn-sm btn-warning" @onclick="() => Editar(cliente.CustomerId)">Editar</button>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    </div>

    <p class="text-muted">Total de clientes: @clientes.Count</p>
}

@code {
    private List<VitaRaiz.Domain.Entities.Customer>? clientes;
    private string searchTerm = "";

    private IEnumerable<VitaRaiz.Domain.Entities.Customer> clientesFiltrados =>
        clientes?.Where(c =>
            string.IsNullOrEmpty(searchTerm) ||
            c.CustomerName.Contains(searchTerm, StringComparison.OrdinalIgnoreCase) ||
            (c.Phone != null && c.Phone.Contains(searchTerm))
        ) ?? Enumerable.Empty<VitaRaiz.Domain.Entities.Customer>();

    protected override async Task OnInitializedAsync()
    {
        await CargarClientes();
    }

    private async Task CargarClientes()
    {
        clientes = await Task.Run(() =>
            Context.Customers
                .Include(c => c.Zone)
                .OrderBy(c => c.CustomerName)
                .ToList()
        );
    }

    private void NuevoCliente()
    {
        // TODO: Abrir modal o navegar a formulario de nuevo cliente
    }

    private void VerDetalle(int customerId)
    {
        // TODO: Navegar a detalle de cliente
    }

    private void Editar(int customerId)
    {
        // TODO: Abrir modal o navegar a formulario de edición
    }
}
