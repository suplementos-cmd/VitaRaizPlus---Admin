@page "/dashboard"
@using MediatR
@using Microsoft.JSInterop
@inject IMediator Mediator
@inject IJSRuntime JS
@rendermode InteractiveServer

<PageTitle>Dashboard - VitaRaiz</PageTitle>

<h1 class="mb-4">📊 Dashboard</h1>

<div class="row">
    <div class="col-md-3">
        <div class="card text-white bg-primary mb-3">
            <div class="card-body">
                <h5 class="card-title">Ventas Hoy</h5>
                <h2>@ventasHoy</h2>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card text-white bg-success mb-3">
            <div class="card-body">
                <h5 class="card-title">Cobros Hoy</h5>
                <h2>$@cobrosHoy.ToString("N2")</h2>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card text-white bg-warning mb-3">
            <div class="card-body">
                <h5 class="card-title">Pendiente</h5>
                <h2>$@pendiente.ToString("N2")</h2>
            </div>
        </div>
    </div>
    <div class="col-md-3">
        <div class="card text-white bg-info mb-3">
            <div class="card-body">
                <h5 class="card-title">Clientes Activos</h5>
                <h2>@clientesActivos</h2>
            </div>
        </div>
    </div>
</div>

<div class="row mt-4">
    <div class="col-md-6">
        <div class="card">
            <div class="card-header">
                <h5>📈 Ventas de la Semana</h5>
            </div>
            <div class="card-body" style="height: 300px;">
                <canvas id="ventasChart"></canvas>
            </div>
        </div>
    </div>
    <div class="col-md-6">
        <div class="card">
            <div class="card-header">
                <h5>💰 Cobros vs Pendiente</h5>
            </div>
            <div class="card-body" style="height: 300px;">
                <canvas id="cobrosChart"></canvas>
            </div>
        </div>
    </div>
</div>

<div class="row mt-4">
    <div class="col-md-12">
        <div class="card">
            <div class="card-header">
                <h5>📊 Tendencia de Cobranza (Últimos 7 días)</h5>
            </div>
            <div class="card-body" style="height: 300px;">
                <canvas id="tendenciaChart"></canvas>
            </div>
        </div>
    </div>
</div>

@code {
    private int ventasHoy = 15;
    private decimal cobrosHoy = 25000.00m;
    private decimal pendiente = 125000.00m;
    private int clientesActivos = 230;

    protected override async Task OnInitializedAsync()
    {
        // TODO: Cargar datos reales desde la base de datos usando MediatR queries
        await Task.CompletedTask;
    }

    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        if (firstRender)
        {
            await RenderCharts();
        }
    }

    private async Task RenderCharts()
    {
        try
        {
            // Gráfico de barras - Ventas por día de la semana
            var ventasLabels = new[] { "Lun", "Mar", "Mié", "Jue", "Vie", "Sáb", "Dom" };
            var ventasData = new[] { 12, 19, 15, 17, 22, 18, 15 };
            await JS.InvokeVoidAsync("chartHelpers.createBarChart", "ventasChart", ventasLabels, ventasData, "Ventas Diarias");

            // Gráfico de donut - Cobros vs Pendiente
            var cobrosLabels = new[] { "Cobrado", "Pendiente" };
            var cobrosData = new[] { 75000, 125000 };
            await JS.InvokeVoidAsync("chartHelpers.createDoughnutChart", "cobrosChart", cobrosLabels, cobrosData);

            // Gráfico de línea - Tendencia de cobranza
            var tendenciaLabels = new[] { "Día 1", "Día 2", "Día 3", "Día 4", "Día 5", "Día 6", "Día 7" };
            var tendenciaData = new[] { 15000, 18000, 12000, 22000, 25000, 19000, 23000 };
            await JS.InvokeVoidAsync("chartHelpers.createLineChart", "tendenciaChart", tendenciaLabels, tendenciaData, "Monto Cobrado ($)");
        }
        catch (Exception ex)
        {
            Console.WriteLine($"Error rendering charts: {ex.Message}");
        }
    }
}
